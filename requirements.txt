# Runtime dependencies (tested with Python 3.12).
# Lyzr Automata is installed separately - see README "Running it":
#   pip install --no-deps --ignore-requires-python lyzr-automata==0.1.3
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30
pydantic>=2.7,<3
pydantic-settings>=2.3
httpx>=0.27
reportlab>=4.0
cryptography>=42
requests>=2.31
